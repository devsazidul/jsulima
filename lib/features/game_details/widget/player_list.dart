import 'package:flutter/material.dart';

class PlayerList extends StatelessWidget {
  final String teamName;
  const PlayerList({super.key, required this.teamName});

  @override
  Widget build(BuildContext context) {
    // Player list is not needed, so return an empty Container or a placeholder
    return Container();
  }
}
